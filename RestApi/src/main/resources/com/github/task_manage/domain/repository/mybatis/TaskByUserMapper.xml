<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.task_manage.domain.repository.mybatis.TaskByUserMapper">
	<!-- マッピング定義 -->
    <resultMap type="com.github.task_manage.domain.model.TaskByUser" id="TaskByUser">
        <id column="task_id" property="taskId" />
        <result column="user_id" property="userId" />
        <result column="task_name" property="taskName" />
        <result column="task_detail" property="taskDetail" />
        <result column="task_done" property="taskDone" />
        <result column="delete_flag" property="deleteFlag" />
    </resultMap>

	<!-- １件insert -->
    <insert id="insertTask" parameterType="com.github.task_manage.domain.model.TaskByUser">
        INSERT INTO task_by_user (
            user_id,
			task_name,
			task_detail)
        VALUES (
            #{userId},
            #{taskName},
            #{taskDetail})
    </insert>

    <!-- 全件検索 -->
    <select id="selectAllTasks" parameterType="int" resultMap="TaskByUser">
    	SELECT
    		task_id,
			task_name,
			task_detail,
			task_done,
			delete_flag
        FROM
            task_by_user
		WHERE
			user_id = #{userId}
    </select>

    <!-- ユーザーの未完了・未削除の全タスク検索 -->
    <select id="selectNotYetTasks" parameterType="int" resultMap="TaskByUser">
    	SELECT
    		task_id,
			task_name,
			task_detail,
			task_done,
			delete_flag
        FROM
            task_by_user
		WHERE
			user_id = #{userId}
			and task_done = 0
			and delete_flag = 0
    </select>

    <!-- 完了した全タスク検索 -->
    <select id="selectDoneTasks" parameterType="int" resultMap="TaskByUser">
    	SELECT
    		task_id,
			task_name,
			task_detail,
			task_done,
			delete_flag
        FROM
            task_by_user
		WHERE
			user_id = #{userId}
			and task_done = 1
			and delete_flag = 0
    </select>

    <!-- 削除した全タスク検索(完了したか否かは問わない) -->
    <select id="selectDeletedTasks" parameterType="int" resultMap="TaskByUser">
    	SELECT
    		task_id,
			task_name,
			task_detail,
			task_done,
			delete_flag
        FROM
            task_by_user
		WHERE
			user_id = #{userId}
			and delete_flag = 1
    </select>

	<!-- １件更新 -->
    <update id="updateTask" parameterType="com.github.task_manage.domain.model.TaskByUser">
        UPDATE task_by_user
        SET
            task_name = #{taskName},
            task_detail = #{taskDetail},
            task_done = #{taskDone},
            delete_flag = #{deleteFlag}
        WHERE task_id = #{taskId}
    </update>

    <!-- １件削除 -->
    <delete id="deleteTask" parameterType="int">
        DELETE FROM task_by_user WHERE task_id = #{taskId}
    </delete>
</mapper>